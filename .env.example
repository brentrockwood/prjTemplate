# [Project Name] Configuration
# ================================
# Non-secret defaults. Safe to commit.
# Override in secrets.env (gitignored).
#
# For secret values (API keys, tokens, passwords), use secrets.env only.

# General
LOG_LEVEL=INFO
DRY_RUN=false

# [Add your project's non-secret config here]
# BATCH_SIZE=20
# MAX_RETRIES=3
